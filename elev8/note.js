const jsMidiId = [[]];

class Note {
	constructor(neckString, fret) {
		this.neckString = neckString;
		this.fret = fret;
		this.jsMidiId = 40;
    }
};

//  DRAWGUITAR.drawCircle(0, 5);
// 	DRAWGUITAR.drawCircle(1, 5);
// 	DRAWGUITAR.drawCircle(2, 5);